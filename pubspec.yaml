name: seltle_app
description: App for chia tien
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.19.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  dio: ^5.1.0
  get_it: ^7.3.0
  shared_preferences: ^2.1.0
  go_router: ^6.5.8
  logger: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mockito: ^5.4.0
  http_mock_adapter: ^0.4.2
  build_runner: ^2.3.3
  flutter_flavorizr: ^2.1.6

flutter:
  uses-material-design: true
flavorizr:
  flavors:
    dev:
      app:
        name: "Chia Tiền App Dev"
      android:
        applicationId: "vn.settle.giangdm.dev"
      ios:
        bundleId: "vn.settle.giangdm.dev"

    prod:
      app:
        name: "Chia Tiền"
      android:
        applicationId: "vn.settle.giangdm.prod"
      ios:
        bundleId: "vn.settle.giangdm.prod"
